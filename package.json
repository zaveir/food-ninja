{
  "dependencies": {
    "three": "^0.174.0",
    "three-mesh-bvh": "^0.9.0"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "scripts": {
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}
